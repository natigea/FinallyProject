@page
@model EcommersProject.Pages.Admin.CategoriesModel
@{
    ViewData["Title"] = L["Admin_Categories"].Value;
}

<div class="row g-4">
    <div class="col-lg-8">
        <div class="card">
            <div class="card-header py-3 d-flex align-items-center justify-content-between">
                <span><i class="bi bi-tags me-2 text-primary"></i>@L["Admin_CatSectionTitle"]</span>
                <span class="badge bg-primary-subtle text-primary">@Model.Categories.Count</span>
            </div>
            <div class="card-body p-0">
                @if (!Model.Categories.Any())
                {
                    <div class="empty-state">
                        <i class="bi bi-tags"></i>
                        @L["Admin_NoCats"]
                    </div>
                }
                else
                {
                    <table class="table table-hover align-middle mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>#</th>
                                <th>@L["Admin_ColName"]</th>
                                <th>@L["Admin_ColDescription"]</th>
                                <th>@L["Admin_ColActions"]</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < Model.Categories.Count; i++)
                            {
                                var c = Model.Categories[i];
                                <tr>
                                    <td class="text-muted small">@(i + 1)</td>
                                    <td class="fw-medium">@c.Name</td>
                                    <td class="text-muted small">@(string.IsNullOrEmpty(c.Description) ? "—" : c.Description)</td>
                                    <td>
                                        <form method="post" asp-page-handler="Delete"
                                              onsubmit="return confirm('@L["Admin_DeleteCatConfirm"]')">
                                            <input type="hidden" name="id" value="@c.Id" />
                                            <button class="btn btn-sm btn-outline-danger btn-action">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card">
            <div class="card-header py-3">
                <i class="bi bi-plus-circle me-2 text-success"></i>@L["Admin_NewCategory"]
            </div>
            <div class="card-body">
                <form method="post" asp-page-handler="Create">
                    <div class="mb-3">
                        <label class="form-label small fw-medium">@L["Admin_CategoryNameLabel"]</label>
                        <input asp-for="NewName" type="text" class="form-control"
                               placeholder="@L["Admin_CategoryNamePh"]" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label small fw-medium">@L["Admin_CategoryDescLabel"]</label>
                        <textarea asp-for="NewDescription" class="form-control" rows="3"
                                  placeholder="@L["Admin_CategoryDescPh"]"></textarea>
                    </div>
                    <button type="submit" class="btn btn-success w-100">
                        <i class="bi bi-plus-lg me-1"></i>@L["Admin_CreateCategory"]
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
