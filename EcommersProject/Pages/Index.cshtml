@page
@{
    Layout = null;
    if (User.Identity?.IsAuthenticated == true)
    {
        var target = User.IsInRole("Admin") ? "/Admin/Dashboard" : "/Seller/Dashboard";
        Response.Redirect(target);
    }
    else
    {
        Response.Redirect("/Auth/Login");
    }
}
